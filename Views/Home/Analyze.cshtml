@model List<string>

@{
    ViewData["Title"] = "Veri Analizi";
}

<div class="container py-5">
    <h2 class="mb-4 text-center">
        <i class="bi bi-activity text-primary"></i> Medical Analyzer - Sonuçlar
    </h2>

    <!-- Analiz Formu -->
    <form asp-action="Analyze" method="post" enctype="multipart/form-data" class="border rounded p-4 mb-5 shadow-sm bg-light">
        <h5 class="mb-3 text-secondary">🔍 Girdi Verileri</h5>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">3D Görüntü</label>
                <input type="file" name="threeDImage" class="form-control" />
            </div>
            <div class="col-md-6">
                <label class="form-label">2D Kesit -16</label>
                <input type="file" name="sectionMinus16" class="form-control" />
            </div>
        </div>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">2D Kesit 00</label>
                <input type="file" name="section00" class="form-control" />
            </div>
            <div class="col-md-6">
                <label class="form-label">2D Kesit +20</label>
                <input type="file" name="section20" class="form-control" />
            </div>
        </div>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">Sayısal Girdi 1</label>
                <input type="number" step="any" name="num1" class="form-control" />
            </div>
            <div class="col-md-6">
                <label class="form-label">Sayısal Girdi 2</label>
                <input type="number" step="any" name="num2" class="form-control" />
            </div>
        </div>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">Metin Girdisi 1</label>
                <input type="text" name="text1" class="form-control" />
            </div>
            <div class="col-md-6">
                <label class="form-label">Metin Girdisi 2</label>
                <input type="text" name="text2" class="form-control" />
            </div>
        </div>

        <div class="text-center">
            <button type="submit" class="btn btn-primary px-4">
                <i class="bi bi-cpu"></i> Analiz Et
            </button>
        </div>
    </form>

    <!-- Analiz Sonuçları -->
    @if (Model != null && Model.Count > 0)
    {
        <div class="card shadow-sm">
            <div class="card-header bg-success text-white">
                <i class="bi bi-bar-chart"></i> Analiz Sonuçları
            </div>
            <div class="card-body">
                <ul class="list-group list-group-flush">
                    @foreach (var item in Model)
                    {
                        <li class="list-group-item">
                            <span>@Html.Raw(item.Replace("\n", "<br/>"))</span>
                        </li>
                    }
                </ul>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-info text-center">
            Henüz analiz yapılmadı. Lütfen formu doldurup gönderin.
        </div>
    }
</div>
